PORT=3001
# Supabase (Phase 1+) — from Dashboard → Project Settings → API
SUPABASE_URL=https://jqrbyjraohyeksmockrj.supabase.co
SUPABASE_SERVICE_ROLE_KEY=replace-with-service-role-key-from-dashboard
# Salt for anonymous_id = hash(fingerprint + salt). Use a long random string.
IDENTITY_SALT=replace-with-long-random-salt
# reCAPTCHA v2 secret key (from Google reCAPTCHA admin)
RECAPTCHA_SECRET_KEY=replace-with-recaptcha-secret
