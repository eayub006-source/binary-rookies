PORT=3001
# Set to 1 to use in-memory demo store (no Supabase needed). Use when project is paused or offline.
# USE_DEMO_STORE=1
# Prisma — Supabase Postgres. Get from Dashboard → Connect → ORMs → Prisma.
# Replace [YOUR-PASSWORD] with your DATABASE PASSWORD (Project Settings → Database), not the API secret.
DATABASE_URL="postgresql://postgres.jqrbyjraohyeksmockrj:[YOUR-PASSWORD]@aws-1-ap-southeast-1.pooler.supabase.com:6543/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres.jqrbyjraohyeksmockrj:[YOUR-PASSWORD]@aws-1-ap-southeast-1.pooler.supabase.com:5432/postgres"
# Salt for anonymous_id = hash(fingerprint + salt).
IDENTITY_SALT=replace-with-long-random-salt
# reCAPTCHA v2 secret. For local/demo, test keys work on any domain (see docs/RECAPTCHA_SETUP.md).
RECAPTCHA_SECRET_KEY=replace-with-recaptcha-secret
